[
  {
    "key": "server",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "SQL Server",
      "placeholder": "",
      "description": "Hostname or IP of SQL Server",
      "required": true
    }
  },
  {
    "key": "database",
    "type": "input",
    "defaultValue": "SSRPM",
    "templateOptions": {
      "label": "Database name",
      "placeholder": "",
      "description": "SSRPM Database name",
      "required": true
    }
  },
  {
    "key": "updateUserOnCorrelate",
    "type": "checkbox",
    "defaultValue": false,
    "templateOptions": {
      "label": "Update User when correlating and mapped data differs from data in SSRPM",
      "required": false,
      "description": "This will update SSRPM users in the create action (not just correlate)."
    }
  },
  {
    "key": "isDebug",
    "type": "checkbox",
    "defaultValue": false,
    "templateOptions": {
      "label": "Toggle debug logging",
      "description": "When toggled, debug logging will be displayed",
      "required": false
    }
  }
]